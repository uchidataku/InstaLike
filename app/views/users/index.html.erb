<% provide(:title, 'ユーザー') %>

<h1>ユーザー</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.user_name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
